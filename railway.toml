# Railway Deployment Configuration
# https://docs.railway.app/deployments

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python init_db.py && gunicorn app:app --timeout 120 --workers 4"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
